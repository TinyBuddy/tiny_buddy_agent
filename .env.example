# TinyBuddy应用环境变量配置示例

# DeepSeek API密钥
DEEPSEEK_API_KEY=sk-xxxxx

# DeepSeek模型名称（可选，默认使用deepseek-chat）
DEEPSEEK_MODEL=deepseek-chat

# 服务器端口配置（可选，默认已在代码中设置）
# HTTP_API_PORT=3142
# WEBSOCKET_PORT=3143
# CONSOLE_UI_PORT=3141

# 日志级别（可选）
# LOG_LEVEL=info

# mem0 API密钥（可选，默认已在代码中设置）
# MEM0_API_KEY=m0-xxx

# mem0启用状态（可选，默认false）
# MEM0_ENABLED=true

# mem0基础URL（可选，默认https://api.mem0.ai）
# MEM0_BASE_URL=https://api.mem0.ai